import React, { ReactNode } from "react";

export default class Header extends React.Component {
    render():ReactNode {
        return (
            <nav>
                header lol
            </nav>
        );
    }
}
